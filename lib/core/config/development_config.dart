abstract final class DevelopmentConfig {
  /// Loads sample records when launching the application locally.
  ///
  /// This must be disabled before production release.
  static const bool useSampleData = true;
}
