import 'package:flutter/material.dart';

class SellInventoryScreen extends StatelessWidget {
  const SellInventoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Sell Inventory'));
  }
}
