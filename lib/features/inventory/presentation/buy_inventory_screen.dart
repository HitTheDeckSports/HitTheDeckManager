import 'package:flutter/material.dart';

class BuyInventoryScreen extends StatelessWidget {
  const BuyInventoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Buy Inventory'));
  }
}
